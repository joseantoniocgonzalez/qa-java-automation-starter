# --- Targets ---
baseUrl=https://www.saucedemo.com/

# --- Browser config ---
# chrome | firefox
browser=chrome
headless=true

# --- Timeouts (milliseconds) ---
timeout.pageLoadMs=30000
timeout.implicitWaitMs=0
timeout.explicitWaitMs=8000

# --- Evidence output (used later) ---
artifacts.dir=screenshots
htmlsource.dir=html-source

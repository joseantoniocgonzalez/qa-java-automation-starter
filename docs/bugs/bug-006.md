# BUG-006 — Data-driven API test returns incorrect id (iteration data not applied)

## Environment
- API: JSONPlaceholder (demo)
- Client: Newman
- OS: Any

## Severity / Priority
- Severity: S3 Minor
- Priority: P2 Medium

## Preconditions
- CSV data file present: posts.csv with postId values

## Steps to reproduce
1. Run Newman with `-d posts.csv`.
2. Execute request `GET /posts/{{postId}}`.

## Expected result
Returned JSON has `id` equal to the `postId` for each iteration.

## Actual result
Returned id does not match iteration data (e.g., always 1).

## Evidence
- Newman output showing mismatch per iteration

## Notes
Usually caused by missing `-d` flag, wrong CSV header, or wrong variable name.

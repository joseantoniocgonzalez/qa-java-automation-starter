name: smoke

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Run Maven tests (UI + Cucumber smoke)
        run: mvn -B test -Dheadless=true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install Newman
        run: npm install -g newman newman-reporter-htmlextra

      - name: Run Newman smoke (API)
        run: |
          mkdir -p reports/newman reports/junit reports/cucumber screenshots html-source
          newman run src/test/resources/postman/collection.json \
            -e src/test/resources/postman/environment.json \
            -r junit,htmlextra \
            --reporter-junit-export reports/junit/newman-smoke.xml \
            --reporter-htmlextra-export reports/newman/newman-smoke.html

      - name: Upload artifacts (reports + evidence)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-artifacts
          if-no-files-found: ignore
          path: |
            reports/**
            screenshots/**
            html-source/**
